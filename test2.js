const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function test() { const instructors = await prisma.user.findMany({ where: { role: 'FACULTY' }, select: { id: true, name: true, facultyProfile: { include: { lab: true } }, fileUploads: { where: { type: 'PHOTO' }, take: 1, orderBy: { uploadedAt: 'desc' } } } }); console.log('success'); } test().catch(e = 
